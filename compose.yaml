services:
  tesla-charger-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-5050}:5000"
    env_file:
      - .env
    environment:
      - PORT=5000
    volumes:
      - ./data:/app/data
      - ./secrets:/app/secrets
    restart: unless-stopped
