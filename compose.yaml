services:
  tesla-charger-status:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5050:5000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./secrets:/app/secrets
    restart: unless-stopped
